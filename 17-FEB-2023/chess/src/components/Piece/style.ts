import { StyleSheet } from "react-native";
import { SIZE } from 'constant'


const styles = StyleSheet.create({

    container: {
        position: "absolute",
        width: SIZE,
        height: SIZE,
    },

    piece: {
        width: SIZE,
        height: SIZE,
    }
})

export { styles };